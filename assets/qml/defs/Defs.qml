import QtQuick 2.0

QtObject {
    id: defs

    property color textColor: "white"

    property color systemColor: "red"
    property color inputColor: "black"
    property color outputColor: "green"
}
